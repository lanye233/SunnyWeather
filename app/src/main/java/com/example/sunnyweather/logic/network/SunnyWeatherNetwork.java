package com.example.sunnyweather.logic.network;

import org.jetbrains.annotations.NotNull;

import kotlin.coroutines.Continuation;
import kotlin.coroutines.CoroutineContext;
import kotlin.coroutines.SafeContinuation;
import kotlin.coroutines.intrinsics.IntrinsicsKt;

/**
 * created by lanye
 * on 1/8/23
 */
public class SunnyWeatherNetwork {
//    private PlaceService placeService = (PlaceService) ServiceCreator.INSTANCE.create(PlaceService.class);
//
//    private searchPlaces(String query) throws InterruptedException {
//        placeService.searchPlaces(query).wait();
//    }
//
//    private Object await(Continuation continuation) {
//        SafeContinuation safeContinuation = new SafeContinuation(IntrinsicsKt.intercepted(continuation));
//        continuation = safeContinuation;
//
//    }

}
